@props(['type' => 'submit' , 'name' , 'variant' => 'primary'])

  <div>
    <button
      type="{{ type }}"
      class="
     @if(variant == 'primary') bg-blue-500 text-white @endif
     @if(variant == 'danger') bg-red-500 text-white @endif
     @if(variant == 'outline') border border-gray-400 text-gray-700 @endif
     px-4 py-2 rounded
    "
    >{{ name }}</button>
  </div>
